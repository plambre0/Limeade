services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: scootsafe
      POSTGRES_PASSWORD: scootsafe
      POSTGRES_DB: scootsafe
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
